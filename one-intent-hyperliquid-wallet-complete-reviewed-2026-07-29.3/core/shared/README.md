# Shared Deterministic Core

共有対象：domain、compiler、policy、canonical hash、state machine、display model。

候補：Rust＋UniFFI／C ABI。ただしplatform securityを共有層へ隠さない。Hyperliquid署名は公式Python SDKのgolden vectorへ必ず照合する。
