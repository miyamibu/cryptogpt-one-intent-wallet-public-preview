# Threat tests

ローカルの prompt injection、boundary leakage、quote／registry／authorization replay、stale state、partial-success、unsafe input のテストは `tests/` にあります。overlay、root/jailbreak、App Attest、Play Integrity、外部 API／SSRF、dependency compromise の実機・CI証跡は未取得です。
